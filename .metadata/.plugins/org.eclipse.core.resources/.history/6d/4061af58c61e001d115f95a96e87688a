package chap10_exception;

// 직접 예외객체를 만들기위해 Exception을 상속받는 클래스 생성
public class MyException extends Exception{
	
	public void myMethode () throws MyException{
		if(true) {
			// 실행할때 마다 항상 MyException 에러를 만드는 메소드
			throw new MyException();
		}
	}
}
